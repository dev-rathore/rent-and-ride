
<div id="navigation">
  <nav class="navbar navbar-expand-lg navbar-dark px-2">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">
        Rent <span class="highlight">&</span> Ride
      </a>

      <!-- Collapse Button for small screens -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active text-center" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-center" href="#">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-center" href="#">Contact</a>
          </li>
          <% if(session.user) { %>
          <li class="nav-item">
            <a class="nav-link text-center" href="/login">Login</a>
          </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link text-center" href="/logout">Logout</a>
            </li>
            <% } %>
          <li class="nav-item">
            <a class="nav-link text-center" href="/register/rider">Register</a>
          </li>
          <% if(session.role == 'rider') { %>
          <li class="nav-item">
            <a class="nav-link text-center" href="/rider-profile/all-orders">All Rides</a>
          </li>
        
          <% } else if(session.role == 'admin'){ %>
            <li class="nav-item">
              <a class="nav-link text-center" href="/logout">Dashboard</a>
            </li>
            <% } else { %>
              <li class="nav-item">
                <a class="nav-link text-center" href="/logout">My vehicles</a>
              </li>
              <% } %>
  
       
          <!-- <li class="nav-item">
            <a class="nav-link text-center" href="/">Logout</a>
          </li> -->
        </ul>
      </div>
    </div>
  </nav>
</div>